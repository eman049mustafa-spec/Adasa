# Adasa
A modern photography blog built with React that simulates a real-world blogging experience. The project focuses on routing, dynamic rendering, search, filtering, and layout switching using local JSON data (no APIs).
